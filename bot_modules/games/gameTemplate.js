/*
   Template for games, to show the necessary routines and explain their function
*/

var gameTalkDef = require('./gameTalkDef.js');

module.exports.preGame = 'Message to the user befor the game starts, rules and such...';
module.exports.gameInfo = 'Message about game version, for startup of game engine';

module.exports.init = function(gameTalkDef)
{
   return run;
};

function run(gameTalkDef)
{
};

module.exports.
