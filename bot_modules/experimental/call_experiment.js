//Wrapper for experimentation - a branch in bot.js will recognize the command "experiment" (prefixed with !, or $!, or whatever),
//and call this function:

//The parameters are placeholders - JavaScript doesn't check for type, so you can give anything
module.exports = function(parameter1, parameter2, parameter3)
{

   //Put experimental code here :-)

}
